#include "spatial_analysis.h"
#include "temporal_analysis.h"

#include <ctime>

using namespace cv;

int main()
{
	vector<GroupingColors> grpColors;
	grpColors.push_back();
null, grpColors);
	while(true)
	{
		while(2.has_next())
		{
			Emage e = 2.next();
			create_output(e, "D://BOPT//NextGenUI//EventShop//EventShop//releases//releaseDec//eventshoplinux//src//main//webapp//results\\2_grouping");
		}
		Sleep(100);
	}
	return 0;
}
